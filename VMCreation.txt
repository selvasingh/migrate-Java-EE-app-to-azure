#install Azure CLI
sudo apt-get install apt-transport-https lsb-release software-properties-common dirmngr -y
AZ_REPO=$(lsb_release -cs)

echo "deb [arch=amd64] https://packages.microsoft.com/repos/azure-cli/ $AZ_REPO main" | sudo tee /etc/apt/sources.list.d/azure-cli.list

sudo apt-key --keyring /etc/apt/trusted.gpg.d/Microsoft.gpg adv \
     --keyserver packages.microsoft.com \
     --recv-keys BC528686B50D79E339D3721CEB3E94ADBE1229CF

sudo apt-get update

sudo apt-get install azure-cli

#install xrdp
export DEBIAN_FRONTEND=noninteractive
#Install LXDE lxde.org and xrdp - (make sure to open 3389 on the NSG of the azure vm)
sudo apt-get update
sudo apt-get install -y lxde
sudo apt-get install -y xrdp
sudo /etc/init.d/xrdp start

#Prepare XWindows System
sed -i 's/allowed_users=console/allowed_users=anybody/' /etc/X11/Xwrapper.config : Not working
sudo touch ~/.xsession
echo "startlxde" > ~/.xsession

# Java8 - refer https://www.weargenius.in/install-zulu-jdk-on-ubuntu/
sudo wget https://repos.azul.com/azure-only/zulu/packages/zulu-8/8u192/zulu-8-azure-jdk_8.33.0.1-8.0.192-linux_x64.tar.gz
#follow the commands listed in the above reference article except the parts following nano.
#Add JAVA_HOME in .profile file
Stuck here !!!
#installed azule from Azure :
sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys 0xB1998361219BD9C9
sudo apt-add-repository "deb http://repos.azul.com/azure-only/zulu/apt stable main"
sudo apt-get -q update
sudo apt-get -y install zulu-8-azure-jdk=8.31.0.2

# Maven
sudo wget http://mirrors.estointernet.in/apache/maven/maven-3/3.6.0/binaries/apache-maven-3.6.0-bin.tar.gz
#add the <installdir>/bin to the PATH variable in .profile. Kill the sesion and relogin. Confirm installation
mvn -v

# PSql : refer https://askubuntu.com/questions/1040765/how-to-install-psql-without-postgres
sudo apt-get install -y postgresql-client
psql --version  


#mysql : 
sudo wget https://dev.mysql.com/get/Downloads/MySQL-Shell/mysql-shell_8.0.13-1ubuntu16.04_amd64.deb
#add the <installdir>/bin to the PATH variable in .profile. Kill the sesion and relogin.

#installing visual studio code which can be launched from accessories
curl https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor > microsoft.gpg
mv microsoft.gpg /etc/apt/trusted.gpg.d/microsoft.gpg
sh -c 'echo "deb [arch=amd64] https://packages.microsoft.com/repos/vscode stable main" > /etc/apt/sources.list.d/vscode.list'
sed -i 's/BIG-REQUESTS/_IG-REQUESTS/' /usr/lib/x86_64-linux-gnu/libxcb.so.1
apt-get update
apt-get install -y code




